# Stage Agent Mode

Install:

```bash
npx skills add getstage/agent-mode
```

Local development:

```bash
npx skills add ./agent-mode --list
```

This repo contains the public installable Stage skill for agents.

Current skill:
- `stage-project-manager`
